package BaekJoon.문자열;

public class summary {
    public static void main(String[] args) {
        /*
        // String 관련

        문자열에서 인덱스번호로 위치 찾기  substring
        S = "text" / result = s.substring(1, 3)  => "ext"  /  s.substring(1) => "ext";

        length;
        배열을 나타낼떈 a.length;  문자열을 나타낼땐 b.length();
        ** 반환값은 int

        new String(charArrayName, startIndex, endIndex)
        char 배열을 String 문자열로 변환

        trim(), strip()
        앞뒤 공백제거 기능
        신버전에서 추가된 strip 이 탭 등 더 많은 공백을 지운다.

        split()
        문자열을 split 내 입력값으로 나눈어 저장하는 기능 " ". "-", ","등 기호도 가능하다.

        StringTokenizer (import 필요함)



        // char 관련

        toCharArray();
        문자열을 char 배열로 저장하는 기능, char[]변수를 생성한다.

        charAt();
        char ch = as.charAt(i), 문자열의 i 번째 값을 char 로 변환하는 기능.

        Character.isLowerCase(ch)  [ isUpperCase  /  isLowerCase ]
        char 변수가 대/소문자인지 구별해주는 함수로,  int 값을 넣으면 unicode 로 변환해서 판별함.
        
        [toUpperCase / toLowerCase]
        is 는 boolean 으로 판별해주는 메서드고, to는 변환해주는 메서드
        
        Character.isDigit()
        숫자인지 구별


        // int 관련

        Integer.parseInt();
        String 변수에 대하여 int로 형변환 하는 기능

        Math.max(a, b)
        입력값으로 받은 숫자 중 가장 큰 숫자


        // Scanner (scanner, sc) 관련

        next??()  [ Byte, Short, Int, Long, Float, Double ]
        해당하는 자료형으로 읽어옴.  String 은 next()

        nextLine()
        \n 을 포함하는 한줄 단위의 입력을 가져와 \n만 버림. (String)
        * next()는 공백을 기준으로 입력받기에 공백 전까지의 단어를 읽고, 개행문자를 가져오지 않는다.
        * 개행문자 = \r\n 같은거

        hasNext()
        boolean /  입력된 것이 있다면 true 로, while 에 사용하기 좋다.

        close()
        Scanner 의 사용 종료

        ** nextInt 다음 nextLine
        nextInt 의 입력 후 엔터를 누르는 것이 nextLine 의 빈값으로 들어갈 수 있기에 주의해야한다.
        Integer.parseInt(nextLine())를 사용하거나 next()를 사용하는 것도 방법.



         */
    }
}
